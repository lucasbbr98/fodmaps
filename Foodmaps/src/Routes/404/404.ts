import { Component } from '@angular/core';

@Component({
    selector: 'agrega-error',
    templateUrl: './404.html',
    styleUrls: ['./404.scss'] //No more need to do require('./script.scss').toString() :)
})
export class NotFoundComponent {
    
}

